#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long long s, d;
    while (cin >> s >> d) {
        long long day = s, total = 0;
        while (total < d) total += day++;
        cout << day - 1 << '\n';
    }
    return 0;
}
