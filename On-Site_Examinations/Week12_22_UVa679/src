#include <iostream>
using namespace std;

int main() {
    int T;
    cin >> T;

    while (T--) {
        int D, I;
        cin >> D >> I;

        int node = 1;

        for (int level = 1; level < D; level++) {
            if (I % 2 == 1) {
                node = node * 2;      // go left
                I = (I + 1) / 2;
            } else {
                node = node * 2 + 1;  // go right
                I = I / 2;
            }
        }

        cout << node << endl;
    }

    return 0;
}