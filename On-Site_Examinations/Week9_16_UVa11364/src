#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int T;
    cin >> T;
    while (T--) {
        int n;
        cin >> n;
        vector<int> x(n);
        for (int &v : x) cin >> v;
        cout << 2 * (*max_element(x.begin(), x.end()) - *min_element(x.begin(), x.end())) << '\n';
    }
    return 0;
}
